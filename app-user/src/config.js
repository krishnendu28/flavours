// Base URL of the Flavours BOB backend.
// Set EXPO_PUBLIC_API_URL in a .env file (see .env.example) for non-local targets.
export const API_BASE = process.env.EXPO_PUBLIC_API_URL || 'http://localhost:3001/api';
